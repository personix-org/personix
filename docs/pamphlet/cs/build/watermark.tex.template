% Watermark preamble — auto-generated by build.sh --watermark.
% Injected into pamphlet-draft.tex right before \begin{document}.
% Czech-language variant of pamphlet-v5-en/build/watermark.tex.template;
% identical mechanics, different default text.

\usepackage{eso-pic}

\definecolor{nwowatermark}{HTML}{B83838}

\newcommand{\nwoDraftBig}{__DRAFT_BIG__}
\newcommand{\nwoDraftBanner}{__DRAFT_BANNER__}

\AddToShipoutPictureFG{%
  \begin{tikzpicture}[remember picture, overlay]
    % Úhel 22°–32°; posun ±9 mm na osu. Koprimní násobitelé zabraňují
    % viditelným opakováním vzorů přes sousední stránky.
    \pgfmathsetmacro{\wmAngle}{int(mod(\value{page}*17,11)) - 5 + 25}%
    \pgfmathsetmacro{\wmDX}{(int(mod(\value{page}*7,9)) - 4) * 3}%
    \pgfmathsetmacro{\wmDY}{(int(mod(\value{page}*13,7)) - 3) * 3}%
    \node[
      rotate=\wmAngle,
      opacity=0.12,
      text=nwowatermark,
      font=\fontsize{34pt}{42pt}\sffamily\bfseries,
      align=center
    ] at ([xshift=\wmDX mm, yshift=\wmDY mm]current page.center)
      {\nwoDraftBig};
    \node[
      anchor=north,
      yshift=-5mm,
      opacity=0.40,
      text=nwowatermark,
      font=\small\sffamily\bfseries
    ] at (current page.north)
      {\nwoDraftBanner};
    \node[
      anchor=south,
      yshift=4mm,
      opacity=0.35,
      text=nwowatermark,
      font=\footnotesize\sffamily\itshape
    ] at (current page.south)
      {\nwoDraftBanner\ \textbullet\ s.~\thepage};
  \end{tikzpicture}%
}
